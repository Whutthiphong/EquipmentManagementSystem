@model IEnumerable<EquipmentManagementSystem.equ_category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.equ_cat_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.equ_cat_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.equ_cat_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.equ_cat_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.equ_cat_id })
        </td>
    </tr>
}

</table>
