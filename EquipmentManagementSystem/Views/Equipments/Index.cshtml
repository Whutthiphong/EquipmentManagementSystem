@model IEnumerable<EquipmentManagementSystem.equipment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.equ_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equ_description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equ_amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equ_status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equ_category.equ_cat_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.equ_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equ_description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equ_amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equ_status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equ_category.equ_cat_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.equ_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.equ_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.equ_id })
        </td>
    </tr>
}

</table>
